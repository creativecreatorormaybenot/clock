name: canvas_clock
description: creativecreatorormaybenot's entry to the Flutter clock challenge.
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_clock_helper:
    path: ../flutter_clock_helper

dev_dependencies:
  pedantic: 1.9.0 # todo remove analysis options

flutter:
  uses-material-design: true
